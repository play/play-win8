<UserControl
    x:Class="Play.Views.SongTileView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Play.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="64"/>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Image x:Name="AlbumArt" Grid.Column="1" VerticalAlignment="Center" Width="128" />

        <StackPanel Orientation="Vertical" Grid.Column="2" VerticalAlignment="Center">
            <TextBlock Grid.Row="1" x:Name="name" FontSize="32" Foreground="Black" />
            <TextBlock Grid.Row="1" x:Name="artist" FontSize="12" Foreground="Gray" />
            <TextBlock Grid.Row="1" x:Name="album" FontSize="12" Foreground="Gray" />
        </StackPanel>

    </Grid>
</UserControl>
